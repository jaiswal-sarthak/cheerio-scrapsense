# 🔐 Login Flow - Visual Diagrams

## Email Magic Link Flow

### ❌ BEFORE (BROKEN)
```
┌─────────────┐
│   /signin   │
│  Enter Email│
│  Click Send │
└──────┬──────┘
       │
       ▼
┌──────────────────────────────┐
│  POST /api/auth/signin/email │
│  body: { email: "..." }      │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────┐
│  Check email in database │
│  Find matching user      │
│  Or create new user      │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Generate verification    │
│ token                    │
│ Send magic link email    │
└──────┬───────────────────┘
       │
       ▼  ← User clicks magic link in email
┌────────────────────────────────┐
│ GET /api/auth/callback/email?  │
│ token=abc123                   │
└──────┬─────────────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Verify token             │
│ Token valid ✓            │
│ Find user                │
│ Create session           │
│ Set session-token cookie │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ ❌ NO REDIRECT LOGIC!    │
│ Browser confused         │
│ Redirects to "/"         │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ GET /                    │
│ Middleware checks auth   │
│ Session not fully ready  │
│ Redirects to /signin     │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│     /signin              │
│  🔄 BACK AT SIGNIN PAGE  │
│  (INFINITE LOOP!)        │
└──────────────────────────┘
```

### ✅ AFTER (FIXED)
```
┌─────────────┐
│   /signin   │
│  Enter Email│
│  Click Send │
└──────┬──────┘
       │
       ▼
┌──────────────────────────────┐
│  POST /api/auth/signin/email │
│  body: { email: "..." }      │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────┐
│  Check email in database │
│  Find matching user      │
│  Or create new user      │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Generate verification    │
│ token                    │
│ Send magic link email    │
└──────┬───────────────────┘
       │
       ▼  ← User clicks magic link in email
┌────────────────────────────────┐
│ GET /api/auth/callback/email?  │
│ token=abc123                   │
└──────┬─────────────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Verify token             │
│ Token valid ✓            │
│ Find user                │
│ Create session           │
│ Set session-token cookie │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ ✅ REDIRECT CALLBACK!    │
│ async redirect({         │
│   url,                   │
│   baseUrl                │
│ }) {                     │
│   return baseUrl +       │
│   "/dashboard"           │
│ }                        │
└──────┬───────────────────┘
       │ Returns: "http://localhost:3000/dashboard"
       ▼
┌──────────────────────────┐
│ Browser navigates to:    │
│ /dashboard               │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ GET /dashboard           │
│ Middleware checks auth   │
│ Session valid ✓          │
│ Render dashboard         │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│    Dashboard Page        │
│   🎉 LOGIN SUCCESS!      │
│   See dashboard content  │
└──────────────────────────┘
```

---

## Google OAuth Flow

### ✅ WORKING (WITH FIX)
```
┌──────────────────┐
│   /signin        │
│ Click "Google"   │
└────────┬─────────┘
         │
         ▼
┌────────────────────────────────┐
│ signIn("google", {             │
│   callbackUrl: "/dashboard"    │
│ })                             │
└────────┬───────────────────────┘
         │
         ▼
┌────────────────────────────────┐
│ Redirect to Google OAuth        │
│ Consent screen                 │
└────────┬───────────────────────┘
         │ ← User approves
         ▼
┌────────────────────────────────┐
│ Google redirects back with code │
│ /api/auth/callback/google?     │
│ code=auth123...                │
└────────┬───────────────────────┘
         │
         ▼
┌────────────────────────────────┐
│ NextAuth exchanges code for    │
│ Google tokens + user info      │
└────────┬───────────────────────┘
         │
         ▼
┌────────────────────────────────┐
│ Create or link account         │
│ Create user if needed          │
│ Create session                 │
└────────┬───────────────────────┘
         │
         ▼
┌────────────────────────────────┐
│ ✅ Redirect callback uses      │
│ callbackUrl: "/dashboard"      │
│ OR fallback: "/dashboard"      │
└────────┬───────────────────────┘
         │
         ▼
┌────────────────────────────────┐
│ Navigate to /dashboard         │
│ 🎉 SUCCESS                     │
│ See dashboard                  │
└────────────────────────────────┘
```

---

## Database Tables Used

### Email Login Uses These Tables:

```
                    ┌─────────────────┐
                    │  verification   │
                    │    _tokens      │
                    │                 │
                    │ • identifier    │
                    │ • token         │
                    │ • expires       │
                    └────────┬────────┘
                             │ (1) Create token
                             │ (2) Find by token
                             │ (3) Delete after use
                             │
         ┌───────────────────┴────────────────┐
         │                                    │
         ▼                                    ▼
    ┌─────────┐                         ┌─────────────┐
    │  users  │                         │ sessions    │
    │         │                         │             │
    │ • id    │◄────────────────────────│ • user_id   │
    │ • email │    (4) Find user        │ • session_  │
    │ • name  │    (5) Create if new    │   token     │
    │ • image │                         │ • expires   │
    └─────────┘                         └─────────────┘
         ▲
         │ (6) Session callback adds user.id
         └─────────────────────────────────
```

### Google OAuth Uses These Tables:

```
         ┌────────────┐
         │ Google API │
         │            │
         │ Returns:   │
         │ • user id  │
         │ • email    │
         │ • name     │
         └──────┬─────┘
                │
         ┌──────┴──────┐
         │             │
         ▼             ▼
    ┌─────────┐    ┌──────────┐
    │  users  │    │ accounts │
    │         │    │          │
    │ • id    │◄───│ • user_id│
    │ • email │    │ • provider
    │ • name  │    │ • provider
    │         │    │  _account_id
    └────┬────┘    │ • tokens │
         │         └──────────┘
         │
         ▼
    ┌─────────────┐
    │ sessions    │
    │             │
    │ • user_id   │──→ (1) Create session
    │ • session_  │    (2) Set cookie
    │   token     │    (3) Redirect
    │ • expires   │
    └─────────────┘
```

---

## Key Files & What Changed

```
your-project/
├── lib/auth/
│   └── options.ts                    ← ✅ UPDATED
│       Added: async redirect callback
│       Purpose: Tell NextAuth where to send users after login
│
├── supabase/
│   └── migrations/
│       └── 0001_init.sql             ← ✅ UPDATED
│           Fixed: RLS policy type casting
│           Purpose: Better database security & type safety
│
├── SOLUTION.md                       ← 📖 Read this
├── CHECKLIST.md                      ← 📋 Use this
├── LOGIN_TROUBLESHOOTING.md          ← 🔍 Debugging guide
└── verify-login-config.js            ← 🧪 Run this to verify
```

---

## State Comparison

### Session State During Login

```
BEFORE redirect fix:
├─ User clicks magic link
├─ Token verified
├─ User exists
├─ Session created in DB ✓
├─ Session token set in cookie ✓
├─ ??? What happens next?
└─ Browser confused → redirects to /signin ❌

AFTER redirect fix:
├─ User clicks magic link
├─ Token verified
├─ User exists
├─ Session created in DB ✓
├─ Session token set in cookie ✓
├─ Redirect callback returns "/dashboard" ✓
├─ Browser navigates to /dashboard
├─ Middleware checks: session valid? YES ✓
├─ Dashboard renders ✓
└─ User sees dashboard ✅
```

---

## Environment Configuration Check

```
.env File Requirements:
┌──────────────────────────────────┐
│ For NextAuth to Work:            │
├──────────────────────────────────┤
│ ✓ NEXTAUTH_URL                   │
│   └─ http://localhost:3000       │
│ ✓ NEXTAUTH_SECRET                │
│   └─ Random secret string        │
├──────────────────────────────────┤
│ For Supabase Adapter:            │
├──────────────────────────────────┤
│ ✓ NEXT_PUBLIC_SUPABASE_URL      │
│ ✓ SUPABASE_SERVICE_ROLE_KEY     │
├──────────────────────────────────┤
│ For Email (Resend):              │
├──────────────────────────────────┤
│ ✓ RESEND_API_KEY                 │
├──────────────────────────────────┤
│ For Google OAuth:                │
├──────────────────────────────────┤
│ ✓ GOOGLE_CLIENT_ID               │
│ ✓ GOOGLE_CLIENT_SECRET           │
└──────────────────────────────────┘

Your Project Status: ✅ All configured
```

---

This should help you understand exactly what was wrong and why the fix works! 🎉
